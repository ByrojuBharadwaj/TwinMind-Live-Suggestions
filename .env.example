# TwinMind Live Suggestions does NOT use server-side environment variables.
#
# Users paste their own Groq API key in the app's Settings modal. The key
# is stored only in their browser's localStorage and sent per-request to
# the Groq API via a header. No .env file is required to run this app.
#
# This file exists only to document that fact for anyone cloning the repo.
